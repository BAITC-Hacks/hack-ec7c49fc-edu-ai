import type { ScenarioInput, SimulationResult } from "@/types/domain";

export function simulateScenario(_input: ScenarioInput): SimulationResult {
  throw new Error(
    "Simulation engine is not implemented yet. Merge feat/simulation-engine first.",
  );
}
